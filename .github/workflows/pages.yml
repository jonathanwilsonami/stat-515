# name: Deploy Quarto to gh-pages
# 
# on:
#   push:
#     branches: [ main ]
#   workflow_dispatch:
# 
# jobs:
#   build-deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
# 
#       - uses: quarto-dev/quarto-actions/setup@v2
# 
#       - uses: r-lib/actions/setup-r@v2
#         with: { use-public-rspm: true }
# 
#       - name: System libs for sf/s2
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y libudunits2-dev libgdal-dev libgeos-dev libproj-dev gdal-bin cmake build-essential pkg-config
# 
#       - uses: r-lib/actions/setup-renv@v2
# 
#       - name: Render
#         run: quarto render
# 
#       - name: Publish to gh-pages
#         uses: peaceiris/actions-gh-pages@v4
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           publish_branch: gh-pages
#           publish_dir: _site   # or docs if you use that
